apiVersion: v1
kind: Service
metadata:
  name: austrac-reporting
  namespace: aml-platform
  labels:
    app: austrac-reporting
spec:
  selector:
    app: austrac-reporting
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
